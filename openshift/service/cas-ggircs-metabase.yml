apiVersion: template.openshift.io/v1
kind: Template
objects:

- apiVersion: v1
  kind: Service
  metadata:
    name: cas-ggircs-metabase
  spec:
    ports:
    - name: cas-ggircs-metabase
      nodePort: 0
      port: 3000
      protocol: TCP
      targetPort: 3000
    selector:
      name: cas-ggircs-metabase
    sessionAffinity: None
    type: ClusterIP
  status:
    loadBalancer: {}

