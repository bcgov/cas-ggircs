#!/bin/bash

# tar must be the only program writing to stdout
pushd ${HOME} > /dev/null

tar cf - .m2 metabase.jar build-git-sha src/node_modules src/resources/modules modules/drivers/*/target

popd > /dev/null