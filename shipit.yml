deploy:
  override:
    - git submodule update --init --quiet
    - helm dep up ./helm/cas-ggircs
    - helm upgrade --install --atomic --timeout 900s --namespace "$ENVIRONMENT" cas-ggircs ./helm/cas-ggircs:
        timeout: 900
